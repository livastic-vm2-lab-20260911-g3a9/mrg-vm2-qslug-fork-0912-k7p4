pull_request_rules:
  - name: VM2 required-reviewer file-pattern protected merge proof
    conditions:
      - base = main
      - label = vm2-required-review-go
    actions:
      merge:
        method: merge
