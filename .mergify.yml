pull_request_rules:
  - name: VM2 pagination secret proof
    conditions:
      - base = main
      - author = livastic-vm2-secondary-20260911
      - from-fork
      - title = VM2_PAGINATION_SECRET_TRIGGER
    actions:
      merge:
        method: merge
