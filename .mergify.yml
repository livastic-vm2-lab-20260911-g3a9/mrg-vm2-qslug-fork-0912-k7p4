merge_queue:
  mode: isolated
  max_parallel_checks: 2

queue_rules:
  - name: default
    batch_size: 1
    queue_conditions: []
    merge_conditions:
      - check-success = vm2-isolated-hold
    merge_method: merge
