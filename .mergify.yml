pull_request_rules:
  - name: VM2 CODEOWNERS differential matrix
    conditions:
      - title ~= ^VM2_CODEOWNERS_MATRIX_
      - label = vm2-codeowners-never-trigger
    actions:
      merge:
        method: merge
