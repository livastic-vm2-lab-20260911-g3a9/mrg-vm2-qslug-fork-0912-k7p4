scopes:
  source:
    manual:
merge_protections_settings:
  auto_merge_conditions: true
queue_rules:
  - name: vm2-buildkite-scope-impact
    batch_size: 1
    merge_method: merge
    queue_conditions:
      - title ~= ^VM2_BK3_QUEUE_
      - check-success = @github-actions/vm2-buildkite-scope-guard
    merge_conditions:
      - title ~= ^VM2_BK3_QUEUE_
      - check-success = @github-actions/vm2-buildkite-scope-guard
  - name: vm2-external-fork-missing-scope
    batch_size: 1
    merge_method: merge
    queue_conditions:
      - title ~= ^VM2_MISSING_SCOPE_
      - check-success = @github-actions/vm2-missing-scope-guard
    merge_conditions:
      - title ~= ^VM2_MISSING_SCOPE_
      - check-success = @github-actions/vm2-missing-scope-guard
      - label = vm2-missing-scope-never-merge
