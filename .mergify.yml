pull_request_rules:
  - name: VM2 classic pagination prerequisite reduction
    conditions:
      - base = main
      - author = livastic-vm2-secondary-20260911
      - from-fork
      - title = VM2_CLASSIC_PAGINATION_TRIGGER
    actions:
      merge:
        method: merge
